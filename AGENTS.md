# Repository Guidelines

## プロジェクト構成
- `src/main.zig` は、現在の MicroZig ビルドで使うファームウェアのエントリーポイントです。
- `src/root.zig` には、現時点ではファームウェアビルドに接続されていない補助コードと単体テスト例があります。
- `build.zig` は Raspberry Pi Pico 向けのファームウェア設定を定義し、`build.zig.zon` はパッケージ情報と `microzig` 依存関係を管理します。
- `cspell.json` には、`microzig` や `raspberrypi` など、このリポジトリ固有の許可語を記録します。
- 生成物は `.zig-cache/` と `zig-out/` に置かれ、どちらも Git 管理対象外です。

## ビルド・テスト・開発コマンド
- `zig build`: `build.zig` に従ってファームウェア成果物を生成します。
- `zig fmt build.zig src/*.zig`: Zig ソースを整形します。
- `zig fmt --check build.zig src/*.zig`: 変更せずに整形状態を検証します。
- `zig test src/root.zig`: 現時点では独立して残っている `root.zig` のテスト例を実行します。

現時点ではトップレベルの `zig build test` ステップやローカル実行ターゲットはありません。再現可能なホスト側ワークフローが必要になったら、`build.zig` に明示的に追加してください。

## コーディング規約
Zig 標準の整形に従い、変更後に `zig fmt` を通してください。既存コードに合わせて、4 スペースのインデント、変数名は `snake_case`、関数名は `camelCase`、テスト名は `test "basic add functionality"` のように振る舞いを表す名前を使います。新しいハードウェア名やライブラリ名を導入した場合は、必要に応じて `cspell.json` も更新してください。

## テスト方針
テストは Zig 標準の `test` ブロックを使います。現状のテスト例は `src/root.zig` に独立して残っています。新しいロジックには焦点を絞ったテストを追加し、可能な限り純粋な処理とハードウェア依存処理を分けて、ボードなしでも検証できる形を保ってください。より広い検証が必要になったら、都度コマンドを増やすより `build.zig` に専用ステップを追加します。

## コミット・PR・Issue
コミットメッセージ、PR、Issue は英語で書いてください。コミット件名は、履歴にある `Add Microzig` のような短い命令形を基本にします。PR や Issue には、後で見返したときに判断の背景が分かるよう、変更内容、試したこと、対象ボードや書き込み手順などの前提を簡潔に残してください。ローカル Git フックでは `git-secrets` が動くため、認証情報やデバイス秘密情報をコミットしないでください。

## エージェント向け指示
Codex とのやり取りと `AGENTS.md` の本文は日本語で行います。ただし、コミットメッセージ、PR、Issue の文面は英語を使います。
このリポジトリは個人の学習用プロジェクトです。過度な抽象化や先回りした設計より、理解しやすい小さな変更と試行錯誤を優先してください。Codex エージェントは支援役に徹し、主に方針の相談、エラー発生時の調査、変更内容のレビューを行います。実装や大きな変更は、明示的な依頼がある場合にのみ進めてください。
